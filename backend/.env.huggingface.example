# HuggingFace Spaces — copy these into the Space's "Variables and secrets" UI.
# GEMINI_API_KEY must be set as a Secret. The rest can be Variables.

GEMINI_API_KEY=your_gemini_key_from_aistudio
USE_SQLITE=true
CHROMA_DIR=/tmp/chroma_db
ENVIRONMENT=production
GEMINI_MODEL=gemini-2.5-flash
